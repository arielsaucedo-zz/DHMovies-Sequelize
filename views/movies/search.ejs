<!DOCTYPE html>
<html lang="es">
<body>

	<!-- todos los productos -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Resultados de la búsqueda: <%= movies.length %></h2>
			</div>
			<!-- <% if ( movies.length == 0 ) { %>
			<div class="col-12">
				<h2 class="products-title" style="color: tomato;">No hay productos que coincidan con esa búsqueda</h2>
			</div>
			<% } else { %> -->
			
                <div class="container">
                    <% movies.forEach(movie => { %>
                    <ul>
                        <li>
                            <h1><%= movie.title %></h1>
                        </li>
                    </ul>
                    <% }) %> 
                </div>
               
           <!--  <% } %> --> 
        </div>  
    </div>  
								
</body>

</html>